---
"alignfirst": minor
---

Standardized ticket variable names across command output and guides. `ticket --next` now reports `CYCLE_LETTER` and `FILE_NUMBER`, with matching uppercase Markdown labels and JSON keys.
