---
"@paleo/alcode": minor
---

`new --catchup` loads the ticket history before the protocol and message. `--message-file <path|->` reads the message from a file or stdin. The prompt reaches the coding agent through stdin. The `catchup` protocol is removed. `--no-ticket` reads `TICKET_ID` from the `alignfirst` report.
